'use client';

import React, { useEffect } from 'react'
import ReactDOM from 'react-dom'
import { install } from 'fetch-timer'
// install();


const PerfSSR = ({
    data = globalThis.perfSSRData
}) => {
}


export default PerfSSR;